const p=require('puppeteer');(async()=>{const b=await p.launch();const pg=await b.newPage();await pg.goto('http://localhost:4173/index.html');await pg.click('#btn-start-diag');await pg.waitForSelector('.answer', {visible:true,timeout:3000}).catch(()=>{});const next=await pg.\\\('#btn-next', el => ({hidden: el.hidden, disabled: el.disabled, display: window.getComputedStyle(el).display})).catch(e=>e.message);console.log(next);await b.close();})()
