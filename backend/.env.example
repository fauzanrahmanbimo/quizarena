# Application Port
PORT=8080

# Primary Railway Database Connection String (Preferred over DB_* variables)
# Railway CLI and environment automatically populate this when you link a MySQL service.
DATABASE_URL=

# Fallback Database Connection variables (Used if DATABASE_URL is missing)
DB_HOST=
DB_USER=
DB_PASSWORD=
DB_NAME=

# Security Secrets
JWT_SECRET=
